<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Desk Array</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #181818;
      color: #e9e9e9;
      min-height: 100vh;
      font-family: 'Inter', Arial, sans-serif;
    }
    .navbar, .footer {
      background: #222;
    }
    .navbar-brand {
      font-weight: bold;
      color: #fff !important;
      letter-spacing: 1px;
    }
    .nav-link {
      color: #ccc !important;
      margin-right: 1rem;
    }
    .nav-link:hover {
      color: #fff !important;
    }
    .search-section {
      padding: 4rem 0 2rem 0;
      text-align: center;
    }
    .search-section h1 {
      font-size: 2.5rem;
      font-weight: 600;
      margin-bottom: 2rem;
    }
    .form-control, .form-select {
      background: #222;
      color: #e9e9e9;
      border: none;
      border-radius: 2rem;
    }
    .btn-search {
      background: #26c6da;
      color: #181818;
      border-radius: 2rem;
      font-weight: 500;
      padding: 0.5rem 2rem;
      margin-left: 0.5rem;
      border: none;
    }
    .featured-section {
      padding: 2rem 0;
    }
    .card {
      background: #222;
      color: #eee;
      border: none;
      border-radius: 1rem;
      box-shadow: 0 2px 12px rgba(0,0,0,0.12);
      margin-bottom: 2rem;
      transition: transform 0.1s;
    }
    .card:hover {
      transform: translateY(-4px) scale(1.03);
    }
    .footer {
      padding: 2rem 0 1rem 0;
      text-align: center;
      color: #888;
      font-size: 0.95rem;
      border-top: 1px solid #2a2a2a;
      margin-top: 3rem;
    }
  </style>
</head>
<body>

  <!-- NAVBAR: replace the static links with Rails helpers -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <%= link_to root_path, class: "navbar-brand" do %>
        <img src="https://cdn.midjourney.com/d64d3314-c8f7-4783-adb4-372aecc8b739/0_1.png"
          alt="Desk Array Logo"
          style="height:60px; vertical-align:middle; margin-right:8px; border-radius:50%;"> Desk Array
      <% end %>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <!-- HOME always goes to root (desks#index) -->
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>

          <% if user_signed_in? %>
            <!-- “List Your Space” goes to new_desk_path -->
            <li class="nav-item">
              <%= link_to "List Your Space", new_desk_path, class: "nav-link" %>
            </li>
            <!-- Show “All Listings” for clarity; optional if root is already index -->
            <li class="nav-item">
              <%= link_to "All Listings", desks_path, class: "nav-link" %>
            </li>
            <!-- Logout (Devise) -->
            <li class="nav-item">
              <%= link_to "Logout", destroy_user_session_path,
                          method: :delete, class: "nav-link" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>


  <section class="search-section">
    <div class="container">
      <h1>Book your desk today</h1>
      <form class="d-flex justify-content-center align-items-center gap-2" style="max-width:600px;margin:auto;">
        <input type="text" class="form-control" placeholder="City or location" style="max-width: 220px;">
        <input type="date" class="form-control" style="max-width: 150px;">
        <button type="submit" class="btn btn-search">Search</button>
      </form>
    </div>
  </section>

  <!-- FEATURED SECTION: replace three static cards with a @desks.each loop -->
  <section class="featured-section">
    <div class="container">
      <h2 class="mb-4" style="font-weight:500;letter-spacing:.5px;">Featured Workspaces</h2>
      <div class="row row-cols-1 row-cols-md-3 g-4">

        <% @desks.each do |desk| %>
          <div class="col">
            <div class="card h-100">
              <%# If you have an ActiveStorage attachment, you can render desk.image here. Otherwise show a placeholder. %>
              <% if desk.image&.attached? %>
                <%= image_tag desk.image.variant(resize_to_limit: [400, 200]), class: "card-img-top", alt: "Desk Image" %>
              <% else %>
                <img src="https://via.placeholder.com/400x200/222/444?text=No+Image" class="card-img-top" alt="No Image">
              <% end %>

              <div class="card-body">
                <h5 class="card-title"><%= desk.address %></h5>
                <p class="card-text"><%= desk.description %></p>
                <p><strong>Location:</strong> <%= desk.location %></p>
                <p><strong>Shared?</strong> <%= desk.shared ? "Yes" : "No" %></p>

                <!-- “Book” button (inside the loop, so `desk` is defined) -->
                <%= link_to "Book",
                            new_desk_booking_path(desk),
                            class: "btn",
                            style: "background: #26c6da; color: #181818; margin-top: 0.5rem;" %>
              </div>

              <div class="card-footer text-muted">
                Listed by: <%= desk.user.name.presence || desk.user.email %>
              </div>
            </div>
          </div>
        <% end %>

      </div>
    </div>
  </section>

  <!-- FOOTER (unchanged) -->
  <footer class="footer">
    <div class="container">
      &copy; 2025 Desk Array &mdash; All rights reserved ·
      <a href="#" style="color:#bbb;text-decoration:none;">Privacy</a> ·
      <a href="#" style="color:#bbb;text-decoration:none;">Contact</a>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
